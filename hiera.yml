---
version: 5

defaults:
  datadir: data
  data_hash: yaml_data

hierarchy:
  - name: "In module hierarchy"
    paths:
      - "%{facts.os.name}-%{facts.os.release.major}.yml"
      - "%{facts.os.name}.yml"
      - "%{facts.os.family}-%{facts.os.release.major}.yml"
      - "%{facts.os.family}.yml"
      - "common.yml"
